// Built-in TypegenPlugins. T9 lands the `kick/db` plugin; T8 will add
// `kick/routes`, `kick/env`, `kick/assets` once the legacy generator is
// carved into per-domain plugins.

export { kickDbTypegen } from './db'
