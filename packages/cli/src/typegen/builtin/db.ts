// The `kick/db` typegen moved to `@forinda/kickjs-db/cli` (it ships on
// `dbCliPlugin` now). Re-exported here for back-compat — existing imports
// from `'../typegen/builtin/db'` keep resolving.
export { kickDbTypegen } from '@forinda/kickjs-db/cli'
