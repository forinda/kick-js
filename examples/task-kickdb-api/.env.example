PORT=3000
NODE_ENV=development

# Postgres connection. Required for `kick db generate`, `kick db migrate`,
# and the runtime `pg.Pool` in src/db/client.ts.
#
# Local dev (matches the docker command in README.md):
DATABASE_URL=postgres://postgres:dev@localhost:5432/postgres
