// Import all WS controllers and services so decorators register.
// Add new controllers here — the entry file only imports this barrel.
import './chat.service'
import './chat.controller'
import './notifications.controller'
