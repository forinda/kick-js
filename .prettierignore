# Build artifacts — generated by package builds, never hand-edited.
**/dist/
**/.kickjs/
**/.turbo/
**/.wireit/
**/coverage/

# DevTools SPA bundle is a Vite build output committed for publish; the
# minified bytes drift run-to-run with esbuild, so keeping it in the
# formatter's path produces noisy diffs and slow `format:check` runs.
packages/devtools/public/

# VitePress generated site
**/.vitepress/dist/
**/.vitepress/cache/

# Generator-emitted SPA assets (icons, etc.)
packages/vscode-extension/resources/icon.png

# Lockfiles and node_modules
**/node_modules/
pnpm-lock.yaml
