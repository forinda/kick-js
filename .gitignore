node_modules/
dist/
.env
coverage/
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.idea/
.vscode/
# Claude Code's machine-local override — leaks usernames / absolute
# paths / per-developer permissions. Each contributor keeps their
# own copy outside source control.
.claude/
.wireit/
.turbo/
*.log
*.tsbuildinfo
**/.vitepress/dist/
**/.vitepress/cache/
RELEASE_NOTES_*.md
benchmark-results.json
benchmarks/results.json
.ignored
superpowers/
*.vsix
# Bundle-size harness scratch dir — written by scripts/bundle-size-check.ts.
# `**/` so the rule fires inside any nested project, not just root.
**/.kickjs/
examples/